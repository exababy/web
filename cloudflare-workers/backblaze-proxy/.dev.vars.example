# Copy to `.dev.vars` (which is gitignored) and fill in. Used by
# `wrangler dev` for the local proxy. Production uses
# `wrangler secret put …` instead — values entered there never live
# on disk.

BUCKET_NAME=5stack 
S3_ENDPOINT=s3.us-east-005.backblazeb2.com

S3_ACCESS_KEY=""
S3_SECRET=""
